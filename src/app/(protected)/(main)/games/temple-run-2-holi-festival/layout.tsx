import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Temple Run 2: Holi Festival' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
